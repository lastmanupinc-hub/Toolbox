# Messaging System
# Project: axis-toolbox
# Generated: 2026-04-14T05:22:14.933Z
# Summary: axis-toolbox is a monorepo built with TypeScript using React. It contains 500 files across 20 top-level directories. It defines 152 domain models.

product:
  name: "axis-toolbox"
  type: "monorepo"
  primary_language: "TypeScript"
  stack: "React"

taglines:
  primary: "axis-toolbox is a monorepo built with TypeScript using React. It contains 500 files across 20 top-level directories. It defines 152 domain models."
  technical: "React monorepo — 500 files, 114,741 lines of TypeScript"
  conceptual: "Built around packages/ (monorepo_packages), apps/ (monorepo_apps), payment-processing-output/ (project_directory)"

value_propositions:
  - id: stack
    headline: "React Expertise"
    detail: "Built with React ^19.1.0 — stack-native patterns throughout."
  - id: api_surface
    headline: "431 API Endpoints"
    detail: "Complete API with 232 GET, 179 POST, 20 DELETE — ready for integration."
  - id: domain_model
    headline: "152 Domain Entities"
    detail: "Rich domain model with AuthContext, EnvSpec, ValidationError, ValidationResult, ZipEntry and 147 more."
  - id: architecture
    headline: "Clean Architecture (0.64 separation)"
    detail: "monorepo, containerized with 1 layer boundaries."
  - id: quality
    headline: "Test-Driven Quality"
    detail: "Tested with vitest across 132 test files."

feature_messages:
  api_surface:
    count: 431
    message: "431 API endpoints ready for integration"
    routes:
      - "GET /v1/health"
      - "POST /v1/accounts"
      - "POST /v1/snapshots"
      - "GET /v1/admin/stats"
      - "GET /v1/admin/accounts"
      - "GET /v1/admin/activity"
      - "GET /llms.txt"
      - "GET /.well-known/skills/index.json"
      - "GET /v1/docs.md"
      - "GET /.well-known/axis.json"
  language_support:
    count: 10
    message: "10 languages detected and analyzed"
    breakdown:
      - "TypeScript: 78,834 lines (69.6%)"
      - "JSON: 12,643 lines (11.2%)"
      - "YAML: 10,876 lines (9.6%)"
      - "Markdown: 9,235 lines (8.1%)"
      - "CSS: 849 lines (0.7%)"
  framework_detection:
    count: 1
    message: "1 frameworks detected with stack-aware output"
    detected:
      - name: "React"
        version: "^19.1.0"
        confidence: 0.95

calls_to_action:
  primary:
    label: "Upload Project"
    context: "Main landing page, empty states"
  secondary:
    label: "View Results"
    context: "After snapshot processing complete"
  api:
    label: "Send Snapshot via API"
    context: "Developer documentation, API reference"
