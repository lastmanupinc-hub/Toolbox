# Messaging System
# Project: axis-iliad
# Generated: 2026-05-07T23:19:37.994Z
# Summary: axis-iliad is a monorepo built with TypeScript using React. It contains 500 files across 16 top-level directories. It defines 202 domain models.

product:
  name: "axis-iliad"
  type: "monorepo"
  primary_language: "TypeScript"
  stack: "React"

taglines:
  primary: "axis-iliad is a monorepo built with TypeScript using React. It contains 500 files across 16 top-level directories. It defines 202 domain models."
  technical: "React monorepo — 500 files, 126,554 lines of TypeScript"
  conceptual: "Built around apps/ (monorepo_apps), packages/ (monorepo_packages), payment-processing-output/ (project_directory)"

value_propositions:
  - id: stack
    headline: "React Expertise"
    detail: "Built with React ^19.1.0 — stack-native patterns throughout."
  - id: api_surface
    headline: "479 API Endpoints"
    detail: "Complete API with 269 GET, 189 POST, 21 DELETE — ready for integration."
  - id: domain_model
    headline: "202 Domain Entities"
    detail: "Rich domain model with AuthContext, EnvSpec, ValidationError, ValidationResult, ZipEntry and 197 more."
  - id: architecture
    headline: "Clean Architecture (0.65 separation)"
    detail: "monorepo, containerized with 1 layer boundaries."
  - id: quality
    headline: "Test-Driven Quality"
    detail: "Tested with vitest across 140 test files."

feature_messages:
  api_surface:
    count: 479
    message: "479 API endpoints ready for integration"
    routes:
      - "GET /v1/health"
      - "POST /v1/accounts"
      - "POST /v1/snapshots"
      - "GET /v1/admin/stats"
      - "GET /v1/admin/accounts"
      - "GET /v1/admin/activity"
      - "GET /llms.txt"
      - "GET /.well-known/skills/index.json"
      - "GET /v1/docs.md"
      - "GET /.well-known/axis.json"
  language_support:
    count: 8
    message: "8 languages detected and analyzed"
    breakdown:
      - "TypeScript: 85,493 lines (73.9%)"
      - "JSON: 11,394 lines (9.8%)"
      - "YAML: 8,584 lines (7.4%)"
      - "Markdown: 7,907 lines (6.8%)"
      - "JavaScript: 1,313 lines (1.1%)"
  framework_detection:
    count: 1
    message: "1 frameworks detected with stack-aware output"
    detected:
      - name: "React"
        version: "^19.1.0"
        confidence: 0.95

calls_to_action:
  primary:
    label: "Upload Project"
    context: "Main landing page, empty states"
  secondary:
    label: "View Results"
    context: "After snapshot processing complete"
  api:
    label: "Send Snapshot via API"
    context: "Developer documentation, API reference"
