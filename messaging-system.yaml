# Messaging System
# Project: axis-toolbox
# Generated: 2026-04-14T00:47:02.572Z
# Summary: axis-toolbox is a monorepo built with TypeScript using React. It contains 500 files across 19 top-level directories. It defines 151 domain models.

product:
  name: "axis-toolbox"
  type: "monorepo"
  primary_language: "TypeScript"
  stack: "React"

taglines:
  primary: "axis-toolbox is a monorepo built with TypeScript using React. It contains 500 files across 19 top-level directories. It defines 151 domain models."
  technical: "React monorepo — 500 files, 117,670 lines of TypeScript"
  conceptual: "Built around packages/ (monorepo_packages), apps/ (monorepo_apps), payment-processing-output/ (project_directory)"

value_propositions:
  - id: stack
    headline: "React Expertise"
    detail: "Built with React ^19.1.0 — stack-native patterns throughout."
  - id: api_surface
    headline: "428 API Endpoints"
    detail: "Complete API with 229 GET, 179 POST, 20 DELETE — ready for integration."
  - id: domain_model
    headline: "151 Domain Entities"
    detail: "Rich domain model with AuthContext, EnvSpec, ValidationError, ValidationResult, ZipEntry and 146 more."
  - id: architecture
    headline: "Clean Architecture (0.64 separation)"
    detail: "monorepo, containerized with 1 layer boundaries."
  - id: quality
    headline: "Test-Driven Quality"
    detail: "Tested with vitest across 131 test files."

feature_messages:
  api_surface:
    count: 428
    message: "428 API endpoints ready for integration"
    routes:
      - "GET /v1/health"
      - "POST /v1/accounts"
      - "POST /v1/snapshots"
      - "GET /v1/admin/stats"
      - "GET /v1/admin/accounts"
      - "GET /v1/admin/activity"
      - "GET /llms.txt"
      - "GET /.well-known/skills/index.json"
      - "GET /v1/docs.md"
      - "GET /.well-known/axis.json"
  language_support:
    count: 8
    message: "8 languages detected and analyzed"
    breakdown:
      - "TypeScript: 78,281 lines (67.3%)"
      - "YAML: 14,287 lines (12.3%)"
      - "JSON: 12,432 lines (10.7%)"
      - "Markdown: 9,559 lines (8.2%)"
      - "CSS: 849 lines (0.7%)"
  framework_detection:
    count: 1
    message: "1 frameworks detected with stack-aware output"
    detected:
      - name: "React"
        version: "^19.1.0"
        confidence: 0.95

calls_to_action:
  primary:
    label: "Upload Project"
    context: "Main landing page, empty states"
  secondary:
    label: "View Results"
    context: "After snapshot processing complete"
  api:
    label: "Send Snapshot via API"
    context: "Developer documentation, API reference"
