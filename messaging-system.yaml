# Messaging System
# Project: axis-toolbox
# Generated: 2026-04-15T20:25:19.955Z
# Summary: axis-toolbox is a monorepo built with TypeScript using React. It contains 500 files across 17 top-level directories. It defines 162 domain models.

product:
  name: "axis-toolbox"
  type: "monorepo"
  primary_language: "TypeScript"
  stack: "React"

taglines:
  primary: "axis-toolbox is a monorepo built with TypeScript using React. It contains 500 files across 17 top-level directories. It defines 162 domain models."
  technical: "React monorepo — 500 files, 116,119 lines of TypeScript"
  conceptual: "Built around apps/ (monorepo_apps), packages/ (monorepo_packages), payment-processing-output/ (project_directory)"

value_propositions:
  - id: stack
    headline: "React Expertise"
    detail: "Built with React ^19.1.0 — stack-native patterns throughout."
  - id: api_surface
    headline: "449 API Endpoints"
    detail: "Complete API with 248 GET, 181 POST, 20 DELETE — ready for integration."
  - id: domain_model
    headline: "162 Domain Entities"
    detail: "Rich domain model with AuthContext, EnvSpec, ValidationError, ValidationResult, ZipEntry and 157 more."
  - id: architecture
    headline: "Clean Architecture (0.65 separation)"
    detail: "monorepo, containerized with 1 layer boundaries."
  - id: quality
    headline: "Test-Driven Quality"
    detail: "Tested with vitest across 140 test files."

feature_messages:
  api_surface:
    count: 449
    message: "449 API endpoints ready for integration"
    routes:
      - "GET /v1/health"
      - "POST /v1/accounts"
      - "POST /v1/snapshots"
      - "GET /v1/admin/stats"
      - "GET /v1/admin/accounts"
      - "GET /v1/admin/activity"
      - "GET /llms.txt"
      - "GET /.well-known/skills/index.json"
      - "GET /v1/docs.md"
      - "GET /.well-known/axis.json"
  language_support:
    count: 8
    message: "8 languages detected and analyzed"
    breakdown:
      - "TypeScript: 81,263 lines (70.9%)"
      - "JSON: 12,048 lines (10.5%)"
      - "YAML: 10,492 lines (9.1%)"
      - "Markdown: 8,415 lines (7.3%)"
      - "JavaScript: 1,456 lines (1.3%)"
  framework_detection:
    count: 1
    message: "1 frameworks detected with stack-aware output"
    detected:
      - name: "React"
        version: "^19.1.0"
        confidence: 0.95

calls_to_action:
  primary:
    label: "Upload Project"
    context: "Main landing page, empty states"
  secondary:
    label: "View Results"
    context: "After snapshot processing complete"
  api:
    label: "Send Snapshot via API"
    context: "Developer documentation, API reference"
