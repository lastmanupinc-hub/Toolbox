{
  "name": "@axis-iliad/sample-package",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "test": "npx vitest run",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "publish": "npm publish --access public"
  },
  "dependencies": {},
  "peerDependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "tsx": "^4.0.0"
  }
}